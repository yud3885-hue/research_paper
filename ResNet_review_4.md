# Deep Residual Learning for Image Recognition (ResNet) 논문 리뷰

- **저자**: Kaiming He, Xiangyu Zhang, Shaoqing Ren, Jian Sun (Microsoft Research)
- **학회**: CVPR 2016
- **키워드**: Residual Learning, Shortcut Connection, Degradation Problem, ILSVRC 2015

---

## 1. Introduction

### 논문이 다루는 분야

이 논문은 이미지 분류를 비롯한 컴퓨터 비전 인식 문제에서 CNN의 깊이(depth)를 어디까지, 어떻게 늘릴 수 있는지를 다룬다. 논문이 나오기 직전 몇 년 동안 이미지 인식 성능을 끌어올린 가장 큰 요인은 사실상 깊이였다. AlexNet에서 VGG(16~19층), GoogLeNet(22층)으로 넘어오면서 층이 깊어질수록 저수준부터 고수준까지의 특징을 더 풍부하게 쌓을 수 있다는 것이 확인됐고, ImageNet 상위권 모델들은 전부 "very deep" 모델이었다. 그래서 자연스럽게 다음 질문이 나온다. 좋은 네트워크를 만드는 일이 그냥 층을 더 쌓는 것만큼 쉬운 일인가?

### 해당 task에서 기존 연구의 한계점

층을 계속 쌓을 때 가장 먼저 걸리는 문제는 gradient vanishing/exploding이었는데, 이건 이미 어느 정도 해결된 상태였다. 적절한 초기화 방법과 batch normalization 덕분에 수십 층짜리 네트워크도 SGD로 수렴은 시킬 수 있게 됐다.

문제는 그다음이었다. 수렴은 하는데, 깊이를 늘리면 정확도가 어느 순간 포화되더니 오히려 **떨어지는** 현상이 나타났다. 논문은 이걸 degradation problem이라고 부른다. 여기서 중요한 포인트는 이게 오버피팅이 아니라는 것이다. 오버피팅이라면 training error는 계속 낮아지고 test error만 올라가야 하는데, 실제로는 56층 네트워크가 20층 네트워크보다 **training error부터** 더 높았다(Fig. 1). 학습 데이터조차 제대로 못 맞추고 있다는 뜻이다.

논문은 이게 왜 이상한 일인지를 아주 직관적인 사고 실험으로 짚는다. 얕은 네트워크가 있고, 거기에 층을 더 붙여서 깊은 네트워크를 만든다고 하자. 추가된 층들이 전부 항등 함수(identity mapping)를 학습하고 나머지 층은 얕은 모델을 그대로 복사하면, 깊은 모델은 최소한 얕은 모델과 **똑같은** 성능은 나와야 한다. 이런 해가 이론적으로 분명히 존재하는데도 현재의 optimizer가 그걸 못 찾고 있다는 것이다. 즉 표현력의 문제가 아니라 최적화의 문제다.

기존 연구들도 이 방향에서 시도가 없지는 않았다. 중간 층에 보조 분류기를 붙이거나(GoogLeNet, DSN), highway network처럼 게이트가 달린 shortcut을 쓰는 방식이 있었다. 그런데 highway network의 게이트는 데이터에 의존하고 파라미터를 가지기 때문에, 게이트가 닫히면 그 경로는 residual이 아닌 함수를 학습하게 된다. 그리고 실제로 100층을 넘어가는 극단적인 깊이에서 정확도 향상을 보여주지는 못했다.

### 논문의 contributions

1. Degradation 문제가 오버피팅이 아니라 최적화 난이도의 문제임을 실험으로 명확히 보였다.
2. 층이 원래 함수 H(x)를 직접 학습하는 대신 잔차 F(x) = H(x) - x를 학습하도록 문제를 다시 세운 residual learning framework를 제안했다.
3. 이를 파라미터가 전혀 추가되지 않는 identity shortcut으로 구현해서, 기존 프레임워크에서 solver 수정 없이 그대로 학습할 수 있게 했다.
4. ImageNet에서 152층 네트워크를 성공적으로 학습시켜 ILSVRC 2015 분류 부문 1위(top-5 error 3.57%)를 기록했고, detection·localization·segmentation까지 5개 트랙을 전부 가져갔다.

---

## 2. Related Work

Related Work는 크게 두 갈래로 정리되어 있다.

**Residual Representations.** 잔차를 다루는 아이디어 자체는 딥러닝 이전부터 있었다는 점을 먼저 깔아둔다. 이미지 검색·분류에서 쓰이던 VLAD는 딕셔너리에 대한 잔차 벡터로 인코딩하는 표현이고, Fisher Vector도 그 확률적 버전으로 볼 수 있다. 벡터 양자화에서도 원본 벡터보다 잔차 벡터를 인코딩하는 게 더 효과적이라고 알려져 있었다. 저수준 비전 쪽에서는 PDE를 푸는 Multigrid 방법이 문제를 여러 스케일의 부분 문제로 쪼개면서 각 부분 문제가 잔차를 담당하게 하고, hierarchical basis preconditioning도 스케일 간 잔차를 변수로 삼는다. 이런 방법들이 잔차의 구조를 모르는 일반 solver보다 훨씬 빨리 수렴한다는 게 알려져 있으니, 문제를 잔차 형태로 다시 쓰는 것 자체가 최적화를 단순하게 만든다는 근거가 된다.

**Shortcut Connections.** 층을 건너뛰는 연결도 오래된 아이디어다. MLP를 학습시킬 때 입력을 출력에 바로 연결하는 선형 층을 두거나, 중간 층을 보조 분류기에 직접 연결해서 gradient 문제를 다루는 방식이 있었다. Inception 층도 shortcut 브랜치와 몇 개의 깊은 브랜치로 구성된다는 점에서 같은 계열이다.

**차별성을 어떻게 표현하는가.** 저자들이 가장 힘을 주는 비교 대상은 동시기 연구인 highway network다. 차이를 두 가지로 명확하게 정리한다. 첫째, highway의 shortcut은 게이트가 있고 파라미터를 가지지만 ResNet의 identity shortcut은 파라미터가 없다. 둘째, highway는 게이트가 닫힐 수 있어서 그 경우 residual이 아닌 함수를 학습하게 되는 반면, ResNet의 shortcut은 절대 닫히지 않기 때문에 모든 정보가 항상 통과하고 층은 항상 잔차만 추가로 학습한다. 여기에 "highway는 100층 이상에서 정확도 향상을 보이지 못했다"는 실험적 한계를 덧붙여서, 자기들 방법이 극단적 깊이에서도 통한다는 점을 대비시킨다.

개인적으로는 Related Work가 "우리 아이디어가 완전히 새롭다"고 주장하지 않고, 오히려 잔차라는 개념이 여러 분야에서 이미 잘 통했다는 걸 근거로 끌어오는 방식이 인상적이었다.

---

## 3. 제안 방법론

### Main Idea

핵심 아이디어는 학습해야 할 대상을 바꾸는 것이다. 몇 개의 층이 원래 학습해야 할 함수를 H(x)라고 하면, 이 층들이 H(x)를 직접 근사하게 두지 않고

```
F(x) := H(x) - x
```

를 학습하게 만든다. 그러면 원래 함수는 `H(x) = F(x) + x`로 복원된다. 두 형태 모두 이론적으로 표현할 수 있는 함수의 집합은 같지만, **학습의 난이도는 다를 수 있다**는 것이 논문의 가설이다.

왜 이게 도움이 되는지는 Introduction에서 나온 사고 실험과 직접 연결된다. 만약 최적의 함수가 항등 함수에 가깝다면, 일반적인 층 여러 개를 쌓아서 항등 함수를 정확히 만들어내는 건 생각보다 어렵다. 반면 잔차 형태에서는 F(x)의 가중치를 그냥 0으로 밀어버리면 항등 함수가 된다. 최적화 관점에서 훨씬 도달하기 쉬운 지점에 해가 놓이는 셈이다. 저자들은 이걸 "항등 함수가 최적이라고 믿는 건 아니지만, 최적해가 항등 함수 쪽에 가깝다면 잔차 형태가 좋은 preconditioning이 된다"고 조심스럽게 표현한다.

### 구현: Identity Mapping by Shortcuts

실제 building block은 아주 단순하다.

```
y = F(x, {W_i}) + x
```

여기서 F는 보통 2~3개의 층으로 이루어진 잔차 함수이고(예: `F = W2 · relu(W1 x)`), 덧셈은 element-wise addition이다. 덧셈 후에 다시 ReLU를 통과시킨다. F가 층 하나뿐이면 그냥 선형 층과 비슷해져서 이득이 없다고 언급하는데, 이런 실패 조건까지 명시해 준 게 재현할 때 도움이 될 것 같다.

입력과 출력의 차원이 다를 때(채널 수가 늘어나거나 stride로 크기가 반으로 줄 때)는 shortcut을 그대로 더할 수 없다. 이 경우 논문은 세 가지 선택지를 둔다.

- **(A)** 부족한 채널을 0으로 패딩해서 차원을 맞춘다. 파라미터가 추가되지 않는다.
- **(B)** 차원이 바뀌는 부분에서만 1×1 convolution(projection) `W_s`를 사용한다.
- **(C)** 모든 shortcut을 projection으로 바꾼다.

여기서 중요한 건 identity shortcut이 파라미터도, 사실상의 연산량도 추가하지 않는다는 점이다. 덕분에 같은 깊이·같은 파라미터 수를 가진 plain network와 residual network를 **공정하게** 비교할 수 있다. 성능 차이가 순전히 shortcut 때문이라고 말할 수 있게 만든 설계라서, 이 부분이 논문의 실험 신뢰도를 크게 올려준다고 느꼈다.

### Network Architectures

ImageNet용 plain baseline은 VGG의 철학을 따라 대부분 3×3 필터를 쓰고, 두 가지 규칙으로 설계된다. 출력 feature map 크기가 같으면 필터 수를 같게 유지하고, 크기가 절반으로 줄면 필터 수를 두 배로 늘려서 층당 연산량을 비슷하게 유지한다. 다운샘플링은 pooling 대신 stride 2 convolution으로 처리하고, 마지막은 global average pooling과 1000-way FC로 끝난다. 이렇게 만든 34층 plain net의 연산량은 3.6 billion FLOPs로, VGG-19(19.6 billion)의 18%밖에 안 된다. 깊다고 무조건 무거운 게 아니라는 걸 보여주는 대목이다.

Residual network는 여기에 shortcut만 얹은 것이다. 구조를 새로 설계한 게 아니라 같은 baseline에 연결선을 추가한 형태라서 비교가 깔끔하다.

### Deeper Bottleneck Architecture

50층 이상으로 갈 때는 학습 시간이 부담이 되기 때문에 block 구조를 바꾼다. 3×3 두 개 대신 1×1 → 3×3 → 1×1 세 개를 쌓고, 앞의 1×1이 채널 수를 줄이고 뒤의 1×1이 다시 복원하는 방식이다. 3×3 convolution이 좁은 차원에서만 동작하게 되어 연산량이 크게 절약된다.

이 구조에서는 identity shortcut이 특히 중요해진다. 만약 여기서 shortcut을 projection으로 바꾸면 shortcut이 고차원 쪽 양 끝에 연결되기 때문에 연산량과 모델 크기가 두 배가 되어버린다. 파라미터가 없는 shortcut이 단순히 "공정한 비교"용이 아니라 실질적인 효율성 요인이라는 점을 설명하는 부분이다.

50/101/152층은 이 bottleneck block의 개수만 늘려서 만든다. 152층 ResNet도 11.3 billion FLOPs로 VGG-16/19(15.3/19.6 billion)보다 여전히 가볍다.

### 학습 설정

ImageNet 기준으로 짧은 변을 [256, 480]에서 랜덤 샘플링해 스케일 augmentation을 하고, 224×224 크롭과 좌우 반전, 색상 augmentation을 사용한다. 모든 convolution 뒤·활성화 앞에 BN을 넣고, dropout은 쓰지 않는다. SGD에 mini-batch 256, learning rate는 0.1에서 시작해 error가 정체되면 10으로 나누고, weight decay 0.0001, momentum 0.9로 최대 60만 iteration을 학습한다. 재현에 필요한 값들이 대부분 적혀 있어서 구현 가능한 수준으로 서술되어 있다고 봐도 될 것 같다.

---

## 4. 실험 및 결과

### Dataset

- **ImageNet 2012** (1000 클래스, train 128만 장, val 5만 장, test 10만 장): 주 실험
- **CIFAR-10** (10 클래스, train 5만 장, test 1만 장): 극단적 깊이에 대한 분석용
- **PASCAL VOC 2007/2012, MS COCO**: 일반화 성능 확인용 object detection

### Baseline

같은 깊이의 **plain network**가 가장 핵심적인 baseline이다. shortcut 유무만 다르고 파라미터 수와 연산량이 동일하기 때문에, 차이가 나면 원인을 shortcut으로 특정할 수 있다. 그 외에 VGG-16, GoogLeNet, PReLU-net, BN-inception 같은 당시 상위권 모델들과, CIFAR-10에서는 FitNet과 Highway network를 비교 대상으로 쓴다.

### 결과

**(1) Degradation은 실제로 일어나고, residual은 그걸 없앤다.**

Plain network에서는 34층이 18층보다 validation error가 높았고, 학습 전 구간에서 training error도 더 높았다. 반면 ResNet에서는 순서가 뒤집혀서 34층이 18층보다 2.8% 좋아졌다.

| 깊이 | plain | ResNet |
|---|---|---|
| 18 layers | 27.94 | 27.88 |
| 34 layers | 28.54 | **25.03** |

*ImageNet validation top-1 error (%). ResNet은 plain 대비 추가 파라미터가 없음.*

저자들은 plain net의 문제가 gradient vanishing 때문일 가능성을 직접 반박한다. BN을 썼기 때문에 forward 신호의 분산이 0이 아니고, backward gradient의 norm도 정상이라는 걸 확인했다는 것이다. 대신 깊은 plain net의 수렴 속도가 지수적으로 느릴 수 있다는 추측을 내놓으면서, 원인 규명은 future work로 남긴다. 여기서 모르는 걸 모른다고 쓴 게 오히려 신뢰가 갔다.

18층에서는 plain과 ResNet의 최종 정확도가 비슷하지만 ResNet이 더 빨리 수렴한다. 깊이가 그렇게 깊지 않으면 SGD가 plain net에서도 괜찮은 해를 찾긴 하는데, residual이 초반 수렴을 도와준다는 해석이다.

**(2) Identity vs. Projection shortcut.**

A(zero-padding) < B(차원 변화 시에만 projection) < C(전부 projection) 순으로 성능이 좋았지만 차이는 작았다. 저자들은 A가 조금 나쁜 이유를 "패딩된 차원에는 잔차 학습이 없기 때문"으로 보고, C가 아주 조금 나은 건 추가된 파라미터 때문으로 본다. 셋 다 plain보다 확실히 좋으므로 projection이 degradation 해결의 필수 요소는 아니라고 결론짓고, 메모리·연산 비용을 아끼기 위해 C는 쓰지 않는다. 성능이 가장 좋은 옵션을 안 쓰겠다고 명시한 판단이 인상적이었다.

**(3) 더 깊게: 50/101/152층.**

| 모델 | top-1 err. | top-5 err. |
|---|---|---|
| VGG-16 | 28.07 | 9.33 |
| plain-34 | 28.54 | 10.02 |
| ResNet-34 (B) | 24.52 | 7.46 |
| ResNet-50 | 22.85 | 6.71 |
| ResNet-101 | 21.75 | 6.05 |
| ResNet-152 | **21.43** | **5.71** |

*ImageNet validation, 10-crop testing (%)*

깊어질수록 계속 좋아지고 degradation이 관찰되지 않는다. 152층 단일 모델의 top-5 error는 4.49%로, 이전 방법들의 **앙상블** 결과보다도 좋았다. 서로 다른 깊이의 모델 6개를 앙상블한 최종 결과가 test set에서 top-5 error 3.57%이고, 이것이 ILSVRC 2015 1위 기록이다.

**(4) CIFAR-10과 1000층 실험.**

CIFAR-10에서는 6n+2층짜리 단순한 구조로 20/32/44/56층을 비교한다. 여기서도 plain net은 깊어질수록 나빠지고 ResNet은 좋아진다. 110층 ResNet은 error 6.43%로 FitNet, Highway보다 좋으면서 파라미터는 더 적었다(1.7M). 참고로 110층은 초기 learning rate 0.1이 너무 커서 0.01로 warm-up을 한 뒤 다시 올리는 방식을 썼다고 밝히는데, 이런 디테일까지 적어준 건 재현 관점에서 고마운 부분이다.

층 응답의 표준편차를 분석한 Fig. 7도 흥미롭다. ResNet의 층 응답이 plain net보다 전반적으로 작았고, 깊어질수록 개별 층의 응답 크기가 더 작아졌다. 잔차 함수가 실제로 0에 가깝게 학습된다는, 3.1절 가설을 뒷받침하는 간접 증거다.

1202층까지 밀어붙인 실험에서는 최적화 자체는 문제없이 되어서 training error가 0.1% 미만까지 내려갔지만, test error는 7.93%로 110층보다 나빴다. 저자들은 이걸 최적화 실패가 아니라 데이터셋 대비 모델이 너무 큰 오버피팅 문제로 본다.

**(5) Detection으로의 전이.**

Faster R-CNN에서 backbone만 VGG-16에서 ResNet-101로 바꿨을 때 PASCAL VOC 2007에서 mAP가 73.2 → 76.4, COCO의 mAP@[.5, .95]가 21.2 → 27.2로 올랐다. 상대적으로 28% 개선이다. 검출 파이프라인은 그대로 두고 표현만 바꿨기 때문에, 이득이 학습된 표현 자체에서 나온다는 주장이 설득력 있다.

---

## 5. 결론 (배운 점)

### 연구의 의의

이 논문의 가장 큰 의의는 "깊이를 늘리면 좋다"는 방향성이 왜 어느 지점부터 막혔는지를 정확히 진단하고, 아주 작은 구조 변경으로 그 벽을 넘었다는 데 있다. 층 하나 추가하는 데 파라미터가 하나도 안 늘어나는 shortcut이 152층, 나아가 1000층까지 학습을 가능하게 만들었다는 게 잘 안 믿길 정도다. 실제로 이후 나온 거의 모든 비전 모델이 residual connection을 기본으로 깔고 있고, Transformer 계열까지도 같은 구조를 쓰고 있으니 영향력은 굳이 설명할 필요가 없을 것 같다.

논문 구성 면에서도 배울 게 많았다. Introduction에서 "깊은 모델은 최소한 얕은 모델만큼은 해야 한다"는 사고 실험 하나로 문제를 정의하고, 그 논리가 3장 방법론과 4장 실험까지 그대로 이어진다. 특히 plain network와 파라미터 수를 완전히 동일하게 맞춰 비교한 설계가 좋았다. 이렇게 해두면 "그냥 파라미터가 늘어서 좋아진 거 아니냐"는 반론이 애초에 성립하지 않는다. 실험을 설계할 때 무엇을 통제해야 주장이 서는지를 보여주는 사례라고 생각한다.

### 한계점 및 아쉬웠던 점

가장 아쉬웠던 건 **왜 잘 되는지에 대한 이론적 설명이 약하다**는 점이다. 논문 스스로도 "identity mapping이 최적이라고 보기는 어렵다", "깊은 plain net의 수렴이 느린 원인은 앞으로 연구할 것"이라고 적어두고 있다. 잔차 형태가 최적화를 쉽게 만든다는 것은 실험으로 충분히 보여줬지만, 그게 손실 함수 지형을 어떻게 바꾸는지에 대한 분석은 없다. (이 부분은 이후 loss landscape 시각화나 identity mapping을 더 깊게 파고든 후속 연구들에서 다뤄진 것으로 알고 있다.)

1202층 실험을 오버피팅으로 정리한 부분도 조금 아쉽다. dropout이나 maxout 같은 강한 regularization을 붙이면 나아질 것 같다고 언급만 하고 실제로 해보지는 않았다. 물론 논문의 초점이 최적화 난이도이지 regularization이 아니라는 건 이해가 되지만, "깊이의 한계는 어디인가"라는 질문을 던져놓고 답을 미룬 느낌은 남는다.

그리고 A/B/C shortcut 비교에서 A가 왜 조금 나쁜지에 대한 설명("패딩된 차원에는 잔차 학습이 없다")은 그럴듯하긴 한데 추가 실험으로 검증되지는 않았다. 이런 식으로 해석만 붙이고 넘어가는 부분이 몇 군데 보였다.

### 배운 점

읽으면서 가장 크게 남은 건, 좋은 아이디어가 꼭 복잡할 필요는 없다는 것이다. 수식으로 보면 `y = F(x) + x` 한 줄이고 구현도 텐서 덧셈 한 번인데, 그게 문제를 보는 관점을 바꿨기 때문에 효과가 컸다. 뭔가를 직접 학습시키기 어려울 때 "기준점을 잡고 그 차이만 학습시킨다"는 발상은 다른 문제에도 써먹을 수 있을 것 같다.

또 하나는 문제를 진단하는 방식이다. 정확도가 떨어지는 걸 보고 바로 오버피팅이라고 결론 내리지 않고 training error를 먼저 확인한 것, gradient vanishing 가능성을 BN과 gradient norm 측정으로 하나씩 배제한 것이 인상적이었다. 원인을 좁혀가는 과정 자체가 해법 설계로 이어졌다는 점에서, 실험이란 게 성능 숫자를 뽑는 일만은 아니라는 걸 다시 느꼈다.

---

## 참고

He, K., Zhang, X., Ren, S., & Sun, J. (2016). *Deep Residual Learning for Image Recognition.* CVPR 2016, pp. 770-778.
